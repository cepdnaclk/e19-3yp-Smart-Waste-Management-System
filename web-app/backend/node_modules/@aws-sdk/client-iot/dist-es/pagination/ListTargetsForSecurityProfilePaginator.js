import { createPaginator } from "@smithy/core";
import { ListTargetsForSecurityProfileCommand, } from "../commands/ListTargetsForSecurityProfileCommand";
import { IoTClient } from "../IoTClient";
export const paginateListTargetsForSecurityProfile = createPaginator(IoTClient, ListTargetsForSecurityProfileCommand, "nextToken", "nextToken", "maxResults");
