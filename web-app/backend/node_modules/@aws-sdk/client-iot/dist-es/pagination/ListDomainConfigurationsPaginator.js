import { createPaginator } from "@smithy/core";
import { ListDomainConfigurationsCommand, } from "../commands/ListDomainConfigurationsCommand";
import { IoTClient } from "../IoTClient";
export const paginateListDomainConfigurations = createPaginator(IoTClient, ListDomainConfigurationsCommand, "marker", "nextMarker", "pageSize");
