import { createPaginator } from "@smithy/core";
import { ListActiveViolationsCommand, } from "../commands/ListActiveViolationsCommand";
import { IoTClient } from "../IoTClient";
export const paginateListActiveViolations = createPaginator(IoTClient, ListActiveViolationsCommand, "nextToken", "nextToken", "maxResults");
