import { createPaginator } from "@smithy/core";
import { ListSecurityProfilesCommand, } from "../commands/ListSecurityProfilesCommand";
import { IoTClient } from "../IoTClient";
export const paginateListSecurityProfiles = createPaginator(IoTClient, ListSecurityProfilesCommand, "nextToken", "nextToken", "maxResults");
